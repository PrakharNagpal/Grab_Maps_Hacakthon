name: friendship_radius
description: Find the fairest meeting point

environment:
  sdk: ^3.3.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter