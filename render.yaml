services:
  - type: web
    name: equal-miles-api
    runtime: docker
    rootDir: server
    dockerfilePath: ./server/Dockerfile
    dockerContext: ./server
    plan: free
    autoDeploy: true
    envVars:
      - key: GRAB_MAPS_API_KEY
        sync: false
